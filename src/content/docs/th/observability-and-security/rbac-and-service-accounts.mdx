---
title: 'RBAC และ Service Account'
description: 'Role, RoleBinding, ClusterRole, และ ClusterRoleBinding กำหนดว่าใครทำอะไรได้บ้างยังไง และ ServiceAccount ให้ตัวตนกับ Pod ยังไง'
sidebar:
  order: 3
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## ไอเดียในหนึ่งประโยค

RBAC แยกการตัดสินใจเรื่องสิทธิ์ออกเป็นสองส่วน คือ "อะไรที่อนุญาต" (Role หรือ ClusterRole) กับ "ใครได้สิทธิ์นั้น" (RoleBinding หรือ ClusterRoleBinding) และทุก Pod ทำงานผ่านตัวตนแบบ ServiceAccount ไม่ว่าจะตั้งค่าไว้เองหรือไม่ก็ตาม

## Role กับ ClusterRole: อะไรที่อนุญาต

RBAC (Role-Based Access Control) อยู่ภายใต้ API group `rbac.authorization.k8s.io/v1` **Role** กำหนดชุดสิทธิ์ที่จำกัดอยู่แค่ namespace เดียว เป็น verb อย่าง `get`, `list`, `watch`, `create`, `update`, `delete` ที่ใช้กับ resource ที่ระบุไว้

```yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: checkout
  name: pod-reader
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
```

**ClusterRole** คือแนวคิดเดียวกันแต่ scope ระดับ cluster ใช้ได้ทั้งกรณีให้สิทธิ์กับ resource ที่เป็น cluster-scoped (เช่น `nodes` หรือ `namespaces`) หรือให้สิทธิ์กับ resource แบบ namespaced ในลักษณะที่นำไปใช้ซ้ำได้ในหลาย namespace ผ่าน RoleBinding หลายตัว

```yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: secret-reader
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list"]
```

ทั้งสองแบบนี้ไม่ได้ให้สิทธิ์อะไรด้วยตัวมันเองเลย Role หรือ ClusterRole เป็นแค่คำอธิบายสิทธิ์ที่ยังไม่ผูกกับใคร จนกว่าจะมีอะไรมา bind มันเข้ากับ subject

## RoleBinding กับ ClusterRoleBinding: ใครได้สิทธิ์นั้น

**RoleBinding** เชื่อม Role (หรือ ClusterRole ที่นำมาใช้ซ้ำใน namespace เดียว) เข้ากับ subject เช่น User, Group, หรือ ServiceAccount ภายใน namespace เดียว

```yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: read-pods
  namespace: checkout
subjects:
  - kind: ServiceAccount
    name: checkout-app
    namespace: checkout
roleRef:
  kind: Role
  name: pod-reader
  apiGroup: rbac.authorization.k8s.io
```

**ClusterRoleBinding** ทำแบบเดียวกันแต่ให้สิทธิ์ของ ClusterRole ทั่วทั้ง cluster ไม่ใช่แค่ namespace เดียว

```yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: read-all-secrets
subjects:
  - kind: Group
    name: platform-team
    apiGroup: rbac.authorization.k8s.io
roleRef:
  kind: ClusterRole
  name: secret-reader
  apiGroup: rbac.authorization.k8s.io
```

```bash
# Check what a given identity can actually do
kubectl auth can-i list pods --namespace checkout --as system:serviceaccount:checkout:checkout-app
kubectl get rolebindings,clusterrolebindings -A -o wide
```

## ServiceAccount: ตัวตนของสิ่งที่รันอยู่ข้างใน Pod

Kubernetes ไม่ได้จัดการ human user เองโดยตรง กลุ่มนั้นต้อง authenticate ผ่าน identity provider ภายนอก เช่น OIDC, cloud IAM integration, หรือ client certificate ที่ authentication layer ของ API server สิ่งที่ Kubernetes จัดการเองโดยตรงคือ **ServiceAccount** ซึ่งเป็นตัวตนของ process ที่รันอยู่ข้างใน Pod

ทุก Pod รันภายใต้ ServiceAccount ตัวใดตัวหนึ่งเสมอ ถ้าไม่ได้ระบุไว้ มันจะรันเป็น ServiceAccount ชื่อ `default` ของ namespace นั้น

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: checkout
  namespace: checkout
spec:
  serviceAccountName: checkout-app
  containers:
    - name: checkout
      image: checkout:2.3.0
```

โดย default token ของ ServiceAccount จะถูก mount เข้าไปใน filesystem ของ Pod อัตโนมัติ เพื่อให้แอปข้างในใช้ authenticate กับ API server ในฐานะตัวตนนั้นได้ ถ้า workload ไม่เคยคุยกับ Kubernetes API เลย token ที่ mount มาให้อัตโนมัตินี้ก็เป็น attack surface ที่ไม่จำเป็น ควรปิดมันไปตรง ๆ

```yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: checkout-app
  namespace: checkout
automountServiceAccountToken: false
```

## Least privilege ในทางปฏิบัติ

ประเด็นทั้งหมดของการแยก Role/ClusterRole ออกจาก RoleBinding/ClusterRoleBinding ก็เพื่อให้จำกัดสิทธิ์ได้แคบ ๆ ง่าย ๆ

- ให้เฉพาะ verb ที่ workload ต้องใช้จริง ๆ (`get`/`list`/`watch` สำหรับ controller ที่อ่านอย่างเดียว ไม่ใช่ `create`/`update`/`delete`)
- ให้สิทธิ์กับ resource ที่ระบุไว้ชัดเจน และถ้าเป็นไปได้ก็ระบุถึงระดับ `resourceNames` แทนที่จะให้ทั้ง resource type
- เลือกใช้ Role แบบ namespaced คู่กับ RoleBinding แทน ClusterRole คู่กับ ClusterRoleBinding เมื่อ workload ต้องการสิทธิ์แค่ใน namespace เดียว
- หลีกเลี่ยงการ bind ServiceAccount ของ workload เข้ากับ ClusterRole ในตัวอย่าง `cluster-admin` เพราะมันให้สิทธิ์ไม่จำกัดกับทุก resource ใน cluster และแทบไม่มี workload ไหนที่จำเป็นต้องใช้ระดับนั้นจริง ๆ

<Mermaid title="ServiceAccount ที่ bind ผ่าน RoleBinding เข้ากับ Role แล้วถูกตรวจสอบโดย API server ทุกครั้งที่มี request" code={rbacDiagram} />

export const rbacDiagram = `flowchart LR
  pod["Pod\\n(identity: ServiceAccount)"] --> req["Request to API server"]
  req --> authz["API server\\nauthorization check"]
  sa["ServiceAccount"] --> rb["RoleBinding"]
  role["Role\\n(verbs on resources)"] --> rb
  rb --> authz
  authz -- "allowed" --> ok["Request proceeds"]
  authz -- "denied" --> deny["403 Forbidden"]`;

<Callout title="สรุปหนึ่งบรรทัด">
RBAC (`rbac.authorization.k8s.io/v1`) แยก "อะไรที่อนุญาต" คือ **Role** (namespaced) หรือ **ClusterRole** (cluster-wide) ที่ระบุ verb บน resource ออกจาก "ใครได้สิทธิ์นั้น" คือ **RoleBinding** หรือ **ClusterRoleBinding** ที่ผูก Role/ClusterRole นั้นเข้ากับ User, Group, หรือ **ServiceAccount** Kubernetes ไม่ได้จัดการ human user เอง แต่จัดการ ServiceAccount โดยตรง ซึ่งให้ตัวตนกับทุก Pod (เป็น `default` ของ namespace ถ้าไม่ได้ระบุ) พร้อม token ที่ mount มาให้อัตโนมัติ เว้นแต่ตั้ง `automountServiceAccountToken: false` ไว้ Least privilege คือการจำกัด Role ให้แคบทั้ง verb, resource, และแม้แต่ `resourceNames` แล้วไม่ bind workload เข้ากับ `cluster-admin` เว้นแต่จำเป็นจริง ๆ
</Callout>

export const quizRbac = [
  {
    q: 'ความต่างสำคัญระหว่าง Role กับ ClusterRole คืออะไร',
    options: [
      'Role ปฏิเสธสิทธิ์ได้อย่างเดียว ส่วน ClusterRole ให้สิทธิ์ได้อย่างเดียว',
      'Role จำกัดอยู่แค่ namespace เดียว ส่วน ClusterRole เป็น cluster-scoped หรือใช้ซ้ำได้ข้าม namespace',
      'ทั้งสองเหมือนกันเป๊ะ แค่ชื่อต่างกัน',
      'ClusterRole ใช้ได้กับ ServiceAccount เท่านั้น ใช้กับ User ไม่ได้',
    ],
    answer: 1,
  },
  {
    q: 'RoleBinding เชื่อมอะไรเข้ากับอะไรจริง ๆ',
    options: [
      'เชื่อม Role สองตัวเข้าด้วยกันเป็น Role เดียว',
      'เชื่อม Namespace เข้ากับ Node',
      'เชื่อม Role (หรือ ClusterRole) เข้ากับ subject เช่น User, Group, หรือ ServiceAccount ภายใน namespace',
      'เชื่อม Pod เข้ากับ resource quota โดยตรง',
    ],
    answer: 2,
  },
  {
    q: 'ทำไม Pod ถึงมีตัวตนแบบ RBAC ทั้งที่ไม่เคยตั้งค่า ServiceAccount ให้มันเองเลย',
    options: [
      'Pod ที่ไม่มีตัวตนถูกกำหนดไว้จะถูกปฏิเสธตั้งแต่ admission',
      'มันรันเป็น default ServiceAccount ของ namespace นั้นโดยอัตโนมัติ',
      'มันสืบทอดตัวตนจากโหนดที่ถูก schedule ไป',
      'ตัวตนแบบ RBAC จะถูกกำหนดก็ต่อเมื่อ Pod เรียก API server ก่อนเท่านั้น',
    ],
    answer: 1,
  },
  {
    q: 'หลัก least privilege ในบริบทของ RBAC หมายความว่ายังไง',
    options: [
      'ทุก workload ควร bind กับ cluster-admin เพื่อความง่าย',
      'ให้แค่ verb และ resource ที่ workload ต้องใช้จริง ๆ แทนที่จะให้สิทธิ์กว้างหรือระดับ cluster',
      'ปิด RBAC ไปเลยเพื่อไม่ให้ตั้งค่าผิด',
      'ให้สิทธิ์เฉพาะ human user เท่านั้น',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="observability-and-security/rbac-and-service-accounts" questions={quizRbac} />

<ProgressTracker client:visible id="observability-and-security/rbac-and-service-accounts" />
